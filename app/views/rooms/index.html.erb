<div id="rooms">
  <table class="table table-hover table-bordered table-light">
    <thead class="table-dark">
      <%= room_head %>
    </thead>
    <tbody>
      <% @rooms.each do |room| %>
        <%= room_row(room) %>
      <% end %>
    </tbody>
  </table>
</div>
